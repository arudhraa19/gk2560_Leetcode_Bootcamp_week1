class Solution(object):
    def containsDuplicate(self, nums):
        x = set()
        for idx in nums:
            if idx in x:
                return True
            else:
                x.add(idx)
